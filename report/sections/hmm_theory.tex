\section{HMM Theory}
In this section, we will present the type of forward algorithm used to compte the likelihood 
and the 5 different HMM models. 
For generel theory on HMM's, see \cite{zucchini2016hidden} and \cite{moler2024statistical} chapter 11. 
All the models implemented are 4 state Gaussian HMMs (emission density is a Gaussian distribution) 
and we have kept the the mean value of state 1 fixed at 400 as this is the baseline $\text{CO}_2$ level in the room 
when no one is present and all windows are closed. This is also shown in the figure \ref{fig:app:co2_data}. 

\subsection{Forward Algorithm} 
The forward algorithm implemented is called recursive filtering 
\cite{moler2024statistical} as it constructs the current 
statedistribution $p(X_t \mid Y_{1:t})$ recursively from the previous 
state distribution $p(X_t \mid Y_{1:t-1})$.
\input{sections/forward_algorithm.tex}

\subsection{Base HMM model}
\subsubsection{Theory}
The base HMM impelemted is a simple Gaussian HMM with 4 states, where 
the transition probabilities are constant and the emission density 
is a Gaussian distribution with mean and variance that depend on 
the state. When optimizing, we set the 
\subsubsection{Results}

